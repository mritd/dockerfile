version: '2'
services:
  nginx:
    image: mritd/nginx
    restart: always
    volumes:
      - /etc/nginx/ssl:/etc/nginx/ssl
      - /var/log/nginx/:/var/log/nginx/
    volumes_from:
      - piwik_app
    links:
      - jekyll
      - idea
      - yumrepo
      - rpm
      - piwik_app
    ports:
      - "80:80"
      - "443:443"
  jekyll:
    image: mritd/jekyll
    restart: always
  idea:
    image: mritd/idea
    restart: always
  yumrepo:
    image: mritd/yumrepo
    restart: always  
  rpm:
    image: mritd/rpmrepository
    restart: always
    volumes:
      - /data/repo:/data/repo
  piwik_db:
    image: mysql:5.7.17
    restart: always
    volumes:
      - /data/piwik/mysql/runtime:/var/lib/mysql
    environment:
      - MYSQL_ROOT_PASSWORD
  piwik_app:
    image: mritd/piwik:3.0.1
    restart: always
    links:
      - piwik_db
    volumes:
      - /data/piwik/config:/var/www/html/config

version: '2'
services:
  nginx:
    image: mritd/nginx
    links:
      - mritd
      - idea
    ports:
      - "80:80"
  mritd:
    image: mritd/mritd
    volumes:
      - /etc/nginx/ssl:/etc/nginx/ssl
      - /var/log/nginx/mritd.me.log:/var/log/nginx/mritd.me.log
  idea:
    image: mritd/idea

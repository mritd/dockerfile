version: '2'
services:
  nginx:
    image: mritd/nginx
    volumes:
      - /etc/nginx/ssl:/etc/nginx/ssl
      - /var/log/nginx/mritd.me.log:/var/log/nginx/mritd.me.log
    links:
      - mritd
      - idea
    ports:
      - "80:80"
      - "443:443"
  mritd:
    image: mritd/mritd
    volumes:
      - /var/log/nginx/mritd.me.log:/var/log/mritd/mritd.me.log
  idea:
    image: mritd/idea
